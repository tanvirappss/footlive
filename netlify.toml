# netlify.toml
# Root configuration for Netlify to build the Next.js Admin Dashboard in the subdirectory

[build]
  base = "admin-dashboard"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
